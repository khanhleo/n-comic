<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->

    <!-- Application theme. -->
    <style name="ProgressBar">
        <item name="android:progressDrawable">@drawable/dark_progress</item>
        <item name="android:minHeight">@dimen/dark_player_progressbar_height</item>
        <item name="android:maxHeight">@dimen/dark_player_progressbar_height</item>
    </style>

</resources>
